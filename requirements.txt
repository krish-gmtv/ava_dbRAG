# Runtime (chat server + precise SQL + exports)
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0
requests>=2.31.0
openpyxl>=3.1.2

# Development / CI
pytest>=8.0.0

# Optional: semantic ingestion and vector search (offline / full stack)
# pinecone-client>=3.0.0
